<Page x:Class="Alarm.ContentListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Alarm"
      xmlns:view ="clr-namespace:Alarm.ViewModels"
      xmlns:pp="clr-namespace:Publisher;assembly=Publisher"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ContentListView"
      d:DataContext="{d:DesignData Source=/DesignSampleData/DesignNewsSample.xaml}">
    <Page.Resources>
        <DataTemplate x:Key="DocumentTemplate" DataType="{x:Type view:DocumentViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock FontSize="20" Text="{Binding Path=Title}"></TextBlock>
                <TextBlock Text="{Binding Path=Summary}" Grid.Row="1"></TextBlock>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ListBox ItemTemplate="{StaticResource DocumentTemplate}" ItemsSource="{Binding Path=Documents}">
        </ListBox>
        </ScrollViewer>
    </Grid>
</Page>
